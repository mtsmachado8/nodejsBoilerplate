{
  "name": "nodejs-boilerplate",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test --parallel",
    "new-version": "lerna version --no-push --conventional-commits",
    "publish": " && lerna publish --from-git",
    "diff": "lerna diff"
  },
  "devDependencies": {
    "lerna": "^3.16.4"
  }
}
